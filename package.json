{
  "name": "searxng-pages",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "wrangler pages dev ."
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}